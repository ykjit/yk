#!/bin/sh

set -e

output=$($1 $2 2>&1 > /dev/null)

echo "--- Begin icmps ---"
echo "${output}" | grep -a -- '--- Begin\|--- End\|icmp' || true
echo "--- End icmps ---"
echo
echo "--- Begin guard-fail-brs ---"
echo "${output}" | grep -a -- '--- Begin\|--- End\|  br.*%guardfail' || true
echo "--- End guard-fail-brs ---"
